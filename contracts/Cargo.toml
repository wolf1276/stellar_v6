[package]
name = "sie-intent-engine"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]
path = "intent_engine.rs"

[dependencies]
soroban-sdk = "22.0.5"

[dev-dependencies]
soroban-sdk = { version = "22.0.5", features = ["testutils"] }
